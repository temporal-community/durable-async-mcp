# ABOUTME: Temporal-backed implementation of the MCP Tasks extension (io.modelcontextprotocol/tasks).
# Generic, reusable across task-augmented MCP servers/clients; distributed as a Temporal Plugin.
# Intentionally import-light: submodules pull `mcp`, which must not load in the workflow sandbox.
